import React from "react";
import { Typography } from "@mui/material";

const GuestJourney: React.FC = () => {
  return <Typography variant="h4">Welcome to the Guest Journey Page</Typography>;
};

export default GuestJourney;
